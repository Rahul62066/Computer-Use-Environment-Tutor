# Example environment variables for this project
# Copy this file to `.env.local` and fill in the real secret values.
# Do NOT commit your real `.env.local` to source control.

# Database Configuration
# Get your connection string from your DB provider (example: Neon, Heroku, Supabase)
# Example format:
# postgresql://<USER>:<PASSWORD>@<HOST>/<DBNAME>?sslmode=require
DATABASE_URL="postgresql://<DB_USER>:<DB_PASSWORD>@<DB_HOST>/<DB_NAME>?sslmode=require"

# NextAuth / Auth Configuration
# Generate a secret: `openssl rand -base64 32` (or another secure generator)
AUTH_SECRET=<YOUR_AUTH_SECRET>
# The public URL where your app runs (local dev example shown)
AUTH_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (if using Google sign-in)
# Create OAuth credentials in Google Cloud Console and paste them here
GOOGLE_CLIENT_ID="<YOUR_GOOGLE_CLIENT_ID>"
GOOGLE_CLIENT_SECRET="<YOUR_GOOGLE_CLIENT_SECRET>"

# Notes:
# - After filling `.env.local`, restart the dev server so env vars take effect.
# - Keep `.env.local` out of version control. Add to `.gitignore` if needed.
